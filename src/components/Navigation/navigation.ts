import { WeatherData } from "../../weatherApiInterfaces";

const navigationRight = document.querySelector(".navigation-right");
const navigationLeft = document.querySelector(".navigation-left");
// Api respond with 3 days forecast, with 0 being initial and current day
let currentDay = 0;

function renderNextDay(data : WeatherData) {

    


}
